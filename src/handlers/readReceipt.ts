import type { IFCAU_API } from '@dongdev/fca-unofficial';
import { ReadReceiptEventType } from '../../types';

export const read_receipt = async (
  api: IFCAU_API,
  event: ReadReceiptEventType
): Promise<void> => {
};
