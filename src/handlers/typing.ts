import type { IFCAU_API } from '@dongdev/fca-unofficial';
import { TypingEventType } from '../../types';

export const typ = async (
  api: IFCAU_API,
  event: TypingEventType
): Promise<void> => {
};
